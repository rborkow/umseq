# P2C cost-curve housekeeping — Luna
Read docs/review-seed-real-requests.md first (especially arithmetic). You own ONLY scripts/cost_curve.py, a new scripts/tests_cost_curve.py, bench/COST-CURVE.md, and generated bench/fig/* outputs. No other edits, no git commits/staging, no SSH, no delegation. Parent owns CHAIN-POSITION concurrently; do not start any integration work. Deadline 2026-09-07 20:00 PDT.

Add a FOURTH software scenario: 'umbam + GPU seed search (PROJECTED, 1.07–1.15×)' to the cost chart/model/memo. It is an UNMEASURED capacity projection atop the model's umbam CPU replaces BAM chain baseline (~148.5 CPU-min/sample), as in the review, not a new measurement and not multiplication of STAR wall by6.6. Keep the three existing scenarios/measurements unchanged. Use a dashed visually distinct line at the conservative1.07 endpoint plus an explicitly labelled light range/envelope to1.15 (or equally unambiguous representation, no unexplained midpoint). Project cpu-min as baseline/factor, capacity as baseline*factor. No extra hardware capex implied. Include projection flag, exact comparator, bounds and assumptions in cost-model.json; don't imply calibrated composed scenarios are directly measured full-pipeline runs. Keep existing Mac and Batch assumptions clear. Memo states integration not measured yet, kernel is accepted, scheduler coverage sets7–15% prize; integration target>=12% STAR CPU reduction, >=8% gates six-sample pipeline measurement. Chart is motivation, not evidence.

Write targeted regression tests BEFORE edits: baseline scenarios unchanged, projected bounds arithmetic/order,4th scenario/dashed style/explicit projection label; subprocess CLI in temp output dir, inspect generated JSON and nonempty images. Use available interpreter with numpy/matplotlib (check environment; /Users/rborkows/projects/ft/venv/bin/python may exist; don't install global packages). Run tests and regenerate actual bench/fig outputs; no fabricated image/data. Keep changes small, existing style, no broad refactor. State exact execution and files in final summary. Parent will inspect the chart and arithmetic.
