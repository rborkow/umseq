# P2C-PROBE-WARP-REVIEW — Luna, independent focused code/measurement review

Workdir /Users/rborkows/projects/uni-rnaseq. Only write NEW .hermes/cards/P2C-PROBE-WARP-REVIEW-RESULT.md. No code edits, commits, SSH, installs, other workers, or other cards. Local tests allowed. Deadline wrapper stops at 2026-09-06 20:00 PDT.

Read .hermes/cards/P2C-PROBE-WARP.md, crates/umseed-probe/PROBE-WARP-IMPLEMENTATION.md and bench/PHASE2C-seed-probe.md. Review delivered warp changes in crates/umgpu/shim/seed_probe.{cu,h}, its FFI/Rust API, crates/umseed-probe/src/main.rs and tests, tracing definitions and usages. Orchestrator already read collective control flow; do an independent quality pass. Both variants use the same pinned synthetic performance-only inputs on the real full index; agreement is not upstream correctness. No biological or pipeline approval requested.

Focus full-warp collectives, uniform returns before shuffles/ballots, masked/out-of-range lanes, partial blocks, all four direction cases, packed-SA decoding, endpoint/findMultRange, logical counters versus actual speculative reads, unchanged thread/CPU path, API lease lifetimes/synchronization/quarantine. Confirm mean_warp_byte_imbalance remains labeled a group-of32 software proxy, not measured hardware divergence (nor actual intra-warp divergence for warp-per-request). Review actual runner .hermes/cards/probe-warp-host4.sh for fair counts/repeats, output consumption, full-SA synthetic limitations, overlap denominators, page provenance, timer brackets and fail-closed measurement.

Verified by orchestrator so far: local cargo tests/clippy pass. Real CUDA compilation succeeds and both kernel SASS bodies have LDGs. Spark host tests pass in fresh private probe-source-v4. A Linux C++ test needed <cstdio> (fixed in tests/probe_warp.cpp); copying Cargo target to a relocated snapshot retained old CARGO_MANIFEST_DIR, so fresh v4 excludes target and the full rebuild/test succeeded. Host1/2 failures preserved. Host3 rejected an orchestrator request for unsupported tiny batch sizes; protocol was NOT weakened. Host4 uses the supported64k/256k/1M/4M x3 and4K64k x3, all with overlap. Focused odd-tail tests are HOST EMULATION; no assertion that these tiny sizes ran on CUDA. Actual host4 measurements are underway; do not claim unseen numerical results. Prior thread measurement10.20x vsCPU20 at256k is archived/verified; all measurements are probe-only.

Return Blocking / Should fix / Nits / tests actually run / scope. If no blocker, approve only this bounded probe implementation/measurement protocol, NOT SEED-CUDA or STAR fidelity. Cite exact path:line. Separate future correctness-ladder requirements from defects in this performance screen. Do not re-open resolved historical tiny replay issues or demand full biological gates to report a synthetic probe.
