# P2C-CACHE-PERF-READY — close measurement gaps, no more STAR core changes

User authorized follow-through 1–7. Full-depth gate now passed on the repaired frozen binary: 78,619,701 pairs, 211,097,418 ordered alignment records; same documented header normalization; positive GPU consumption, zero strict/fault/rejected counters and zero live charges at finish. Evidence bench/evidence/integrate-full-depth/lat-close/*.json. This is not performance evidence.

You are Terra. Read AGENTS.md, bench/PHASE2C-followthrough-tools.md, current cache_policy_bench.py / measurement_runner.py / measurement_common.py / test_cache_policy_bench.py / test_perf_runner.py and relevant benchmark skill if available. Source beats card silently. No SSH, GPU, real datasets, secrets, commits or model changes. Orchestrator runs the host experiments. Own ONLY bench/star-integrate/cache_policy_bench.py, measurement_runner.py, test_cache_policy_bench.py, test_perf_runner.py; may CREATE a small host_observation.py and its test. CREATE bench/PHASE2C-cache-perf-ready.md. Do not modify measurement_common.py, full-depth runner, core/generator, board or others' docs. Preserve existing changed files. Format readable multiline Python before finishing (Black when available); clang-format/rustfmt if applicable. Bounded correction, not a harness framework.

Remaining faults observed by orchestrator:
- cache runner never sets a sidecar path or checks actual GPU consumption/faults;
- fixed AB order; repeats/timeout are not rejected when nonpositive;
- no live process/system memory evidence, so cannot decide whether retaining page cache increases reclaim/swap;
- measurement_runner._env omits explicit THP/cache flags and metadata says unchanged. It must accept and record the selected policy rather than benchmark an implicit default. Keep strict off. Existing profiler puts /usr/bin/time INSIDE perf record and wrapper timing separately: preserve this.

Deliver minimum needed for real experiments:
1. Cache causal screen: same accepted integrated binary, THP=1, strict=0, explicit eviction off/on, warm-off run before EACH measured arm outside timer, alternate AB/BA per repeat, 3 default repeats. Distinct sidecars for warm and measured runs, require one row with positive gpu_consumed and zero batch_faults/rejected/strict mismatch counters. Existing zero live-charge fields must be checked when expected from this final binary. Fail closed on bad/missing rows. Preserve every output; no rm/deletion.
2. Consecutive mode: clearly define how it samples consecutive same-policy invocations, not merely AB transitions. If redesign would expand scope, restrict/disable misleading mode with clear error and document causal-only readiness. Primary acceptance is causal screen.
3. Add small host observation around measured runs: timestamped actual target PID (not time/timeout wrapper, verify /proc/PID/exe), VmRSS/VmHWM/VmSwap and process I/O; host MemAvailable/Cached/SwapFree and pswpin/pswpout/pgmajfault before/during/after. Label system counters as system-wide. If target sampling never succeeds, fail measurement rather than assert no swap. Missing Linux procfs may be explicitly unavailable on Mac tests, not zeros. Store raw rows incrementally in each stage's fresh root. Keep monitor overhead low (e.g. 1s), outside target GNU-time accounting where possible; no numerical performance claim from tests.
4. Durable raw timing summary per completed arm (actual GNU fields), exact argv/environment/binary identity, and after sidecar validation record acceptance. Failures retain stage evidence; do not lose completed rows if later arm fails. Existing run_stage writes per-stage timing; reuse it rather than duplicate its subprocess logic if practical.
5. Performance runner: explicit CLI eviction 0|1 and THP=1, emitted provenance and per-stage environment. No policy selected by inference. Preserve 3-arm order and profiler event/inner timing. Sidecar validation shared locally if useful but no cross-owner changes.
6. Tests FIRST: RED/GREEN for these actual gaps; fake-process protocol tests must execute runner entry paths and prove missing/unclean sidecar rejection, no warmup in measured row, AB/BA order, environment scrubbing, nonpositive parameters, sampler failure visibility and durable early results. No tests that only search source text. Test pure proc parsing with recorded-format fixtures; never call fixture numbers benchmark results. Run existing four runner/source test suites and shell syntax, no skips for required logic.

Final report: exact files, commands/results, supported host invocation, observed limitations. Do not call this measured or independently accepted. One bounded delivery then stop; orchestrator reviews and runs against ef22723c51540aa2a12178da48f5eb0c3df759965ab7e594f7f6257efba38b6b. No expensive new abstraction or unrelated cleanup.
