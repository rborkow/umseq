# P2C-FOLLOWTHROUGH-TOOLS-REVIEW — independent review and bounded repairs

You are Luna, independent reviewer of Terra tooling delivery (uncommitted). User authorized tasks 1–7. Do not launch real benchmarks/SSH, commit/push, touch secrets/data/runs or change core coordinator/window code. Another Terra actively fixes LAT/PREFETCH, owns core C++ and coordinator/window tests. Orchestrator owns KANBAN/cards/evidence and full_depth_preflight.py, verify_round9_index.py, run_full_depth_stock.py. Read AGENTS.md and bench/PHASE2C-followthrough-tools.md. Source beats card silently; no gate relaxation.

Own review output docs/review-followthrough-tools.md plus the original worker tooling paths ONLY:
bench/star-integrate/{run_full_depth.py,test_full_depth_runner.py,measurement_common.py,cache_policy_bench.py,test_cache_policy_bench.py,perf_differential.sh,run_timing_host.sh,test_perf_runner.py,make_star_integrate.py,test_source_patch.py}
bench/PHASE2C-followthrough-tools.md
Can CREATE bench/star-integrate/measurement_runner.py if replacing shell plumbing with Python simplifies safe execution.

First independently review spec and execution, record concrete issues. Then fix bounded tooling defects with RED/GREEN tests; separate original findings from corrections you authored. Orchestrator will verify your fixes (you cannot independently approve your own corrections). Format readable multiline Python and C++; clang-format/rustfmt where relevant. No broad framework. Do not claim host timings or byte identity from fake tests.

## Observed shortcomings requiring real checks
1. perf_differential.sh ANALYSIS claims cpu-clock but command has no -e cpu-clock. perf report --sort ...tid is unsupported on this host (known from earlier work). Use perf script leaf stacks with tid/ip/sym/dso, or supported outputs; failed extraction is NOT success, no `|| true` hiding missing deliverables. Keep unprofiled timings separate. Keep children accounting separate from STAR-only profile denominator. Profile full event list explicitly and self not inclusive.
2. perf runs fixed stock/bypass/gpu order; timing rotates but neither prewarms index before each arm. Need controlled warmth OUTSIDE timed interval, explicit cache policy, exact flags recorded. Three rotated repeats by default timing; profiling reduced-slice read limit explicit, not guessed old gate. No global cache/policy edits. Prewarm files directly rather than full untimed mapping of every arm if sufficient, verify complete reads and record warm preparation.
3. No GPU sidecar requested in perf/timing. Add and require positive consumption and clean fault counters before accepting performance row. Strict off explicitly. Binary hashes/provenance and real exits/status required; no inference from a wrapper marker. Unsupported profiler extraction should fail with diagnostic retaining raw data.
4. Cache screen currently one off/held/on group, no repeats, held duplicates off, no memory/reclaim sampling. Narrow requirement: repeated paired eviction off/on, advice fixed explicit on, GPU mode fixed; causal warm before each arm versus separate consecutive sequence semantics, actual STAR pid sampling not time parent. Record startup/mapping labels only if reliably measurable, otherwise do NOT invent stage CPU. RSS/swap/process identity/smaps proof and system reclaim counters sufficient to decide policy. Tests need drive executable runner behavior, not only bash -n.
5. Full-depth runner provisional plural mismatch bug was fixed by Terra before finishing; reverify singular required fields fail closed on missing/mismatch. It now passes shlex.join to comparator, but actual checker command_paths uses literal split(" ") and STAR stores its own argv without shell quoting. Do not modify comparator to accommodate runner; validate supported argument form or preserve exact known STAR header serialization, prove through real checker fixtures. Accepted inputs have no whitespace. Missing pair-count and failed stage tests must be added; positive baseline first, intended diagnostic rejection not arbitrary nonzero.
6. Hash binaries and checker always (small), bind input verification manifest explicitly rather than --hash-inputs also disabling executable identity. Do not rehash 30GB mapping indices inside timings.
7. Env isolation must include all inherited STAR_INTEGRATE* knobs, not only five selected ones; explicit known flags for each arm. Review lock/timeout handling and malformed argv (extra/missing mate values) before launching anything.

## Avoid rerunning the already-running full-depth stock golden
Orchestrator started a standalone stock stage while core repair is underway, output `/home/rborkows/uni-rnaseq-probe-lab/full-depth-stock-20260908`. It is NOT parity acceptance. It records argv.json, preflight.json, diagnostic-time.tsv, exit.code, status `PASS_STOCK_ONLY_NOT_PARITY` only after success and expected pairs, plus Aligned.out.sam/SJ/Log.final. Input preflight local copy `bench/evidence/integrate-full-depth/preflight-20260908.json`: both full files MD5 verified and each 78619701 four-line FASTQ records, stock sha f84493ef8c9a6d39e60264c0c9e0c6da388e8a455e6c21c1a1b9a61f52662a29. Exact full inputs /home/rborkows/uni-rnaseq/data/samples/ERR188140/ERR188140_{1,2}.fastq.gz. Orchestrator scripts are read-only references to the stock artifact protocol.

Add OPTIONAL --reuse-stock pointing to that immutable existing result, only if success marker/exit/count, actual executable identity and exact current input paths/verified identities and argv agree. Require stock arguments match candidate parameters apart from executable/output prefix. No silent reuse of old 20M golden. Reused outputs stay immutable; preserve original stock argv for comparator normalization. Fresh integrated output and unchanged checker; fresh-stock default still supported. Test rejection of wrong pair count, changed args, missing success, stale executable/input identity. Full expected-pair assertion derives from independent manifest, not what stock claims. Do not hash enormous SAM for gratuitous proof; streaming unchanged checker is the output check.

## Verify and report
Run exact focused tooling suite; zero skips. Add controlled fake subprocess fixtures that cover successful protocol, failed exit, timeout/signal, absent sidecar, malformed fields and wrong comparison. Synthetic fixtures are TEST DATA, never benchmark evidence. Run generator ABI/source tests without editing concurrent C++ files. `git diff --check`. Report actual commands/results and host-only checks still outstanding. No new task-6 optimization or model changes.
