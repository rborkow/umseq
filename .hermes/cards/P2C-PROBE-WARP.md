# P2C-PROBE-WARP — Astra, conditional performance-only continuation

Workdir /Users/rborkows/projects/uni-rnaseq. Read original .hermes/cards/P2C-SEED-GPU-PROBE.md, P2C-ROADMAP.md and crates/umseed-probe/PROBE-IMPLEMENTATION.md. The orchestrator built your delivered thread implementation on Spark: actual CUDA build/tests pass, kernel SASS60 LDGs, source manifest/binary archived. See bench/evidence/seed-probe-build-host1. Full-index huge-page thread measurements completed: bench/evidence/seed-probe-measure-host1/probe-huge.tsv (all12 isolated+12 overlap rows), end marker COMPLETE, CPU20/GPU outputs and counters identical. Median GPU/CPU20 throughput ratios ~9.99,10.20,10.17,9.94 at64k,256k,1M,4M. This satisfies your <=3x CPU wall gate for implementing the second variant. 4K control is running on Spark; no remote actions from you.

SPLIT is complete and biological parity verified on20M and5M pairs: ~99.79% compared bytes are inner in both, ~93.64% outer requests inner. NO direct extension request type needed. Counter overhead+31.8%CPU on20M is reported, never adjusted; bytes!=CPU fraction. Actual inner meanN~50 and binary-loop iterations~5.36 differ from PROBE's SA_SEARCH_FULL~35 loops; preserve the explicit synthetic/optimistic proof-of-capability labeling. This is still NOT a correctness gate and not SEED-CUDA.

You own ONLY probe-specific existing/new code in crates/umseed-probe, crates/umgpu/shim/seed_probe*, and existing probe-specific API/FFI sections of umgpu/src/{cuda.rs,stub.rs,lib.rs,ffi.rs} (inspect actual symbols/paths first), plus probe build.rs stanza if needed. No umem edits, unrelated GPU changes, evidence rewrites, commits, SSH/network, or other downstream implementation. Format with rustfmt/clang-format. Existing thread behavior and CPU control MUST remain identical.

Implement --variant warp: one warp collaborates on one request, lane-parallel sequence comparisons with a ballot/first-mismatch reduction, preserving packed-SA unpack, GstrandMask, reverse coordinates and all four directions in the current search, including endpoint/findMultRange paths. No lane can exit alone ahead of a full-warp collective, and out-of-range comparison lanes must never issue loads. Partial final blocks and non-multiple-of32 request counts need safe handling. Return EXACT same outputs and logical counters as existing CPU/thread; full-warp speculative reads beyond first mismatch are actual extra loads and MUST be documented as not reflected in the logical compared-byte metric. No inappropriate claim of hardware divergence: existing mean_warp_byte_imbalance is a software group-of32 request-cost proxy, not an activity counter (and isn't actual intra-warp imbalance for the warp-per-request variant).

Preserve same actual lease path and synchronization, GPU context failure/quarantine, no device allocations/copies, CPU20 same buffers, all CLI provenance/warmup/timing/overlap/page verification checks. Do NOT weaken output/counter validation to fit the warp kernel. Add focused host-side regressions/protocol coverage and compiler sanity checks available locally; orchestrator will compile/run onGB10 under lock. Deliver a short PROBE-WARP-IMPLEMENTATION.md with CLI and test results, explicitly host performance NOT RUN. Stop after a bounded implementation; do not independently claim speedup. User downstream/Spark authorization ends conservatively2026-09-06 20:00 PDT, wrapper enforces it. If stuck after~2 substantive attempts on same issue, report the real blocker rather than expanding scope.
