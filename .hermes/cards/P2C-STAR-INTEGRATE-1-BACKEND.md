# INTEGRATE-1 backend completion — Terra, same card, disjoint Rust only

Astra's contract is frozen at bench/star-integrate/CONTRACT.md and usi.h. One Terra worker is concurrently completing C++ STAR scheduler/generator under bench/star-integrate ONLY; do not touch those files. Parent owns run_host.py. This is implementation completion, NOT another review round. ONE Astra review occurs after both deliver. No SSH/commits/services/GPU jobs. Deadline Sept7 20:00PDT.

OWN ONLY crates/umstar/**, and minimal umgpu API correction if absolutely needed to satisfy EXISTING synchronous transport's documented reclamation, with regression. Do NOT edit kernel/search or probe index code; parent just corrected a real SA identity defect in crates/umseed-probe/src/index.rs (ProbeResident.sa_file_bytes now retains exact loaded file bytes) and umstar::identity_for. The old reconstructed extent lost3 bytes on the actual25,352,264,482-byte SA. Regression went RED/GREEN;3 tests pass. Preserve that fix.

Read current backend, actual umgpu seed_probe/reclaim and umem Buf/lease teardown definitions. Complete practical frozen contract with TDD, no re-opening held owned-search/fault/cap-matrix cards. Current issues to resolve:
- Fresh four huge allocations each batch, discard recovered reads/requests/output/stats; contract calls for bounded owned capacity reuse after synchronous completion. Implement reusable capacity without caller-pointer retention. API must slice logical n/read_bytes despite capacity.
- FFI checks incomplete: clear(error) occurs before overlap validation; error/input overlap unchecked; pointer/end/isize bounds and alignment must precede unsafe slices; reverse start can exceed read_len. Contract admits trusted live pointers, not arbitrary invalid-address probing, but validate lengths/ranges/overlap. Make unsafe Rust call obligations explicit without changing C ABI. Epoch mismatch returns correct ID and n=0 validates epoch/context.
- Inspect uncertain completion/panic ownership: panic says disabled without actually setting disabled; don't free potentially in-flight memory or claim quarantine without real existing umem mechanism. Use actual seed_probe_reclaim semantics, don't invent APIs. Catch panics; any GPU/init failure must remain visible/no fakeCUDA. Null-safe destroy as contract.
- No-CUDA builds should report unsupported before huge full-index loading when appropriate. Index profile explicitly strand32/sparse1 and identities checked. Init allocations/map placement vs identity failures distinguished reasonably. Existing loader validates pinned index, SAindex dropped after identity.

Tests exercise actual C entrypoints with small valid CPU buffers/context test seams (never label test backend actual GPU): null/zero/max/overflow/alignment/overlap, failed init output state, domain incl reverse start, result count/status order, buffer reuse and quarantine/disabled behavior as feasible using existing transport fixture helpers. No substantial new mockframework/hardening project. Run cargo test -p umstar and cargo clippy -p umstar --all-targets -- -D warnings, format. DO NOT fix lints by globally disabling warnings; unsafe FFI attributes/documentation legitimate. Preserve borrowed-storage safety and owned-buffer lifetime across draining errors. Optional diagnostics exposed only existing sidecar mechanism, do not alter frozenABI to addtimings. Report actual CUDA verification pending parent. Deliver working backend code/tests, no more spine-only report.