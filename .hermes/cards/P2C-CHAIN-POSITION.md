# P2C-CHAIN-POSITION — Terra, counters only

Read docs/review-seed-real-requests.md (e3b6cee) FIRST and docs/STAR-INTEGRATE-DESIGN.md. Implement ONLY under main bench/chain-position/ (create it); no edits elsewhere, no commits, no SSH or host measurements, no delegation. No SSIR capture/replay, no probe or kernel changes, no integration. Parent runs host and owns reporting/board. Deadline parent-enforced 2026-09-07 20:00 PDT.

Verified baseline tooling is /Users/rborkows/projects/uni-rnaseq-seed/experiments/star-seed/replay/{make_split_capture.py,split_capture_impl.hpp,split_capture_hooks.hpp,real_index_oracle.py,seed_split_parity.py}. Read it and pinned STAR /private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source/{ReadAlign_mapOneRead.cpp,ReadAlign_maxMappableLength2strands.cpp,SuffixArrayFuns.cpp}. The seed worktree is read-only to you: do NOT modify held artifacts. Create a thin private-counter preparation extension of this verified tooling (accept --tooling and --source paths); preserve existing counters and their summary unmodified if feasible. No instrumentation strings guessed from memory. Reuse full tagged-source inventory checks. Actual original source has both call sites and a seedSearchLmax>0 optional path; don't read uninitialized Lmapped there; reject unsupported active fixed-length profile or attribute it explicitly. Default verified profile is seedSearchLmax=0.

Deliver a working private build patcher, formatted C++ attribution hooks/implementation as needed, targeted RED/GREEN source-patching and aggregation tests, a Python summarizer, and a short IMPLEMENTATION.md listing precise run/compile commands. No stubs. Compile actual patched STAR translation units locally if possible using Homebrew libomp include, and exercise portable aggregation tests. Work allocation is per worker, no per-byte mutex/map operations. Do not instrument all reads into a giant file; aggregate per-worker counters and emit a bounded JSON/TSV sidecar at completion. Request attribution must include Lmapped, istart, iDir, ip, and reverse-istart0 suppression (actual source flagDirMap decision, do not change/fix STAR's expression). Joint inner requests/compared bytes/dependent gathers by (Lmapped==0,istart,iDir); retain enough additional attribution to audit ip/suppression and offsets. Gather means each inner comparator/SA fetch, including endpoints and findMultRange, not just binary loop trips. Reconcile aggregates to existing split inner calls, compared bytes and requests.

Primary answer: inner gathers at Lmapped==0 / all inner gathers. Also classify actual requests against hypothetical {initial + grid20} in this SAME counters run, so a <50% result doesn't require regenerating the run. grid20 candidates are offsets 0/20/40/... from each directional split edge, with STOCK prefix narrowing. Derive offset/matching rule from real Shift/seedLength and split edge; ensure full actual inner tuple would agree with the candidate at that position, not just L_in==14. No actual speculative GPU or extra search required: document why candidate stock-prefix inputs match or label any unresolved limitation. Avoid double-counting overlap initial/grid. Report initial, grid-only added work, union, adaptive tail. Only recommend grid if initial gather share<0.5. Suppressed reverse chains have no actual requests: record their opportunity/suppression counts separately, never invent actual work for them. Tests must include multi-piece reads, both directions, suppressed reverse, adaptive steps at/on/off20, overlap initial/grid and zero-work denominators.

Host runner will use full20M ERR188140 pair with original SPLIT flags, existing source snapshot and stock parity oracle, one counters mapping then unchanged seed_split_parity.py. No new guard/cap-matrix/fault harness. Parent can supply a fresh host baseline if required for parity but prefer unchanged accepted stock biological artifacts. Finish with paths/tests and precise remaining host-only work.
