# P2C-LAT-CONTRACT-CLOSE — independent review + missing runtime regressions

You are Astra, correctness specialist. User authorized tasks 1–7. Terra delivered partial fixes in uncommitted core files; see docs/review-integrate-lat-prefetch.md (original independent review) and bench/PHASE2C-lat-prefetch-fix.md (honest partial handoff). This continuation closes the remaining contract gap, NOT a second broad review/research exercise.

Read AGENTS.md. Source beats card silently, stop only source/golden conflict. No SSH, GPU, production data, credentials, commit/push. Another Luna worker owns generator, source-patch test, measurement/full-depth/cache/perf runners. Do not edit those. Orchestrator owns board/cards/host artifacts. You own ONLY:
bench/star-integrate/{star_integrate.cpp,star_integrate.hpp,star_integrate_window.cpp,test_coordinator.cpp,test_coordinator.py,test_window_prefix.cpp,test_window_prefix.py}
CREATE bench/star-integrate/test_window_contract.cpp and test_window_contract.py
CREATE docs/review-integrate-lat-prefetch-fix.md
UPDATE bench/PHASE2C-lat-prefetch-fix.md
Format with clang-format/rustfmt before finishing, readable multiline fixtures. Existing tests may compile current generator; any generated behavioral test should pin/export accepted 89f005c generator or freeze its hash (concurrent generator change only adds eviction policy, not mapChunk call order). No modifying or relaxing the golden comparator.

## What is already verified / not
Original B1 independent scratch probe after finish: 40000 queued, live_requests 40000, SIGABRT destroyed mutex. Orchestrator personally reran /tmp/lat-review-destruction and reproduced -6 exit. Terra adds shutdown drain all queues outside State::mu and repo shutdown-drain regression. Verify with ordinary teardown including delayed-backend scenario; do not merely reorder pool members or _Exit.
Historical B2 is already fixed in accepted HEAD. Current-refusal regression added. Original S1–S3 stream issues and S4 missing real pool reuse/generated consumer harness remain crucial. Terra changed code but explicitly did NOT add test_window_contract harness or tail/rollback injection tests. S5 only changes window attribution and once-per-chain stats, not separate CPU-resolved admission counter.

## Narrow review and complete tests/fixes
1. Independently inspect Terra diffs for B1 and S1–S5, record which are fixed versus remaining. If you repair a residual, mark that as YOUR implementation for orchestrator final review, not independently approved by you.
2. Restore genuine pool reuse/reset regression (third submission reacquires exact pointer only after all owners release; retained shared_ptr prevents reuse), separate from current/next rotation. Assert fields and rebuilt pointer ownership, charges.
3. Build a targeted runtime fixture executing actual prepare_window + coordinator and generated pre-oneRead / readLoad handoff. The prior test_window_prefix.cpp never calls prepare_window; generated-hook compile object is not linked. No hand-constructed frames that bypass the budget-break bug. Local real pinned STAR source exists `/private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source`, read-only. Use its actual stream/readLoad semantics. Wire necessary narrow stubs for backend/index only, document exactly which real generated path runs. No broad capacity matrix.
4. Required cases: at least three distinct paired chunk records and a REAL post-read byte/candidate budget rejection, frame/end ordinal+positions after last admitted record, rotation and every stock record unchanged; partial final window/empty successor, each mate EOF/fail, mismatched mate termination, injected failed second-mate rollback with expected fatal diagnostic; admission refusal; next chunk on same worker; forward gaps and stale overshoot through pre-oneRead call order.
5. Orchestrator concern to test, not assumed bug: window_remaining receives previous stock ordinal, but current code skips frame.ordinal < ordinal, not equality. A stale cursor equal to previous stock ordinal may remain one read behind forever: actual oneRead advances before exact handoff. Prove recovery on the generated sequence, not a hand-called post-read ordinal. Also test no-successor behavior: lookahead_start false must not restart prefetch at current real stream while a tail window remains live, creating duplicate lookahead. If confirmed, fix minimally with RED/GREEN.
6. S5: preserve compatible fields but separately count CPU-resolved admission fallback vs actual device stops; make counters' meaning explicit. Verify per-chain stats charged once and next-window retirement attributed correctly. Do not claim a disjoint partition unless every publication/retirement race is accounted for. Existing omissions may be documented if no partition claimed; no expensive fast-path bookkeeping. Add small tests exercising multistep hit stats and completed-invalid admission fallback.

## Acceptance
Use executable RED against the pre-fix source for each new regression where practical, then GREEN current implementation. Existing tests + ABI compile; ordinary process destruction and ASan shutdown test. Strict host slice/full-depth parity and performance remain NOT RUN and are orchestrator's. Explicitly state if a requested test cannot be implemented and the exact source dependency preventing it; 'left for follow-through' without cause is not completion. Stop at a verified targeted fix/test set; no extra hardening, code quality sweeps or docs outside named files.

Deliver review + final handoff with B1/B2/S1–S5 checklist, commands and results, exact files, and remaining host gates. Current full-depth stock golden already completed, 78619701 pairs. It waits only for a reviewed final candidate and runner; do not regenerate or access it.
