# PROBE measurement review — Luna, read-only sources, bounded pass
Workdir /Users/rborkows/projects/uni-rnaseq. Read .hermes/cards/P2C-SEED-GPU-PROBE.md, P2C-ROADMAP.md and crates/umseed-probe/PROBE-IMPLEMENTATION.md. Review newly implemented PROBE crate and umgpu probe additions ONLY for defects that invalidate measurement or unsafe normal execution. Write findings to .hermes/cards/P2C-PROBE-MEASUREMENT-REVIEW.md; no source edits, no SSH/network/commits, no benchmark runs. Run local tests/small adversarial probes if needed. Don't start a correctness ladder or demand PE/full-index oracle before the intentionally performance-only probe. Astra authored source; independent review is for basic evidence soundness, not release approval.
Orchestrator independently reran Mac cargo test -p umseed-probe -p umgpu (10 tests) and strict clippy successfully. On Spark private snapshot /home/rborkows/uni-rnaseq-probe-lab/probe-source-v1: actual offline release --featurescuda compilation passed, 7 probe host tests passed, cuobjdump SASS for probe_thread_kernel contains60LDG instructions. Evidence /home/rborkows/uni-rnaseq-probe-lab/probe-build-host1. No GPU request run or throughput yet. SPLIT full-index STAR now running under lock, results will decide inner vsdirect boundary; don't assume it. You remain local.
Focus: faithful CPU control over same input without per-request alloc/index scan; complete CPU/GPU tuples/status consumed; correct end-to-end wall versus CUDA events; per-batch medians/repeats; disjoint-half overlap timing/comparator; THP/4K actually differs and allocator coverage; residence/pointer/zero-copy claim caveats; plain bounds/FFI errors affecting valid requests. The Rust/C++ differential is NOT independent STAR oracle, and SA_SEARCH_FULL grid is NOT scheduler capture. Check that no dishonest throughput claim follows from it. Flag any source-script provenance flaw, but orchestrator runs a separate private host wrapper rather than the worker's hardcoded remote-main script. Do not turn the script's unexecuted defaults into a kernel blocker.
Output succinct findings with path:line, reproducer/why it changes the gate, necessary fix; distinguish blockers from caveats already disclosed. If no blockers, say so at the bounded PROBE scope. Cutoff supervisor20:00 LosAngeles2026-09-06. No downstream work. Budget one focused pass and finish promptly.
