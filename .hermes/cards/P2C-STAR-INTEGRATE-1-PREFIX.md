# Same INTEGRATE-1 card — Terra, finish ONE leaf: pure prefix/window producer

OWN ONLY bench/star-integrate/star_integrate_window.cpp and a new test_window_prefix.cpp (or test_window_prefix.py). NO edits to header/generator/coordinator/Rust/docs/run_host.py. Another Terra worker owns coordinator, generator, begin_map and tests. Parent has added the shared internal boundary to star_integrate.hpp; use it unchanged. NO SSH/commits. Cutoff20:00PDT. This task is intentionally narrow: finish this producer end-to-end, not another skeleton.

Current file is a source-compiled readLoad/mate-combine/qualitySplit scan but discards all prepared data. Source is /private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source, inspect ReadAlign_maxMappableLength2strands.cpp lines22–96 (gSAsparseD1 only) to implement PURE CPU prefix narrowing. It returns false for stock short/direct branches and true ONLY with the exact inner tuple; never call alignment/search/storeAligns. Respect stock dirR=(iDir==0), start offsets and markers/range/prefix, including equality known prefix. No grid/adaptive continuations.

Finish prepare_window: if window_remaining() return; if !setup(chunk.P,chunk.mapGen) return (inspect exact genome member, RA.mapGen is available). Build std::vector<WindowRead> with one frame per real parsed record, even zero candidates. Frame.ordinal=actual readLoad ordinal; a,b=stock exact combined bytes/positionwise complement. Fill frame.candidates with InnerCall from prefix, piece=ip, fragment=splitR[2][ip], distance0, Nstart/Lstart/istart actual, generation0 (coordinator fills window generation). Current bounded scan32768pairs uses stream save/restore and copied clipper vectors; preserve those. Publish vector via submit_window(std::move(frames)) ONCE, after restoring streams. EOF aftersome valid records must break/submit them, not return/drop all. Unsupported record/profile can safely causeCPU fallback; do not corrupt streams. Fix any source discrepancy in qualitySplit/mateclip while here with targeted test, not a new parser.

REMOVE empty begin_map from this file; its definition now belongs to coordinator worker. Do not call lookup/submit per candidate. No per-read full ReadAlign replicas. No speculative qualHist/stats/RNG/storeAligns.

Tests should exercise actual producer prefix vs real upstream CPU prefix oracle on a bounded fixture/test shim, not merely string presence. At minimum compile actual file against pinnedSTAR headers using LLVM/libomp and document clear untestedGPU status. Existing Mac syntax command in prior IMPLEMENT-3.log/IMPLEMENTATION; generate current private root with make_star_integrate.py if needed, no source-pin changes. Use clang-format absolute /opt/homebrew/opt/llvm/bin/clang-format. Do NOT stop until frames with real inner tuples are passed to submit_window. Return changed files and actual tests.