# INTEGRATE-1 continuation3 — Terra: ONLY missing lookahead, not another rendezvous

Your second delivery honestly says it cannot form64k: each worker blocks at one inner call. Correct: that is NOT mechanism(a). Replace it with a real lookahead window now. Backend is being finished by a different Terra worker; DO NOT TOUCH Rust/Cargo/frozen ABI/run_host.py. You own bench/star-integrate C++/generator/tests/IMPLEMENTATION only. Same user authorization20:00PDT, no host/commits/review. All source/contract paths from previous cards unchanged. Do not repeat completed hash/ABI/backend exploration.

Concrete source-backed implementation route (inspect exact symbols/types, source wins):
1. Hook ReadAlignChunk::mapChunk, before RA->oneRead in its stock while-loop. Per-worker thread_local window valid for that already-owned chunk. If no remaining window, scan bounded lookahead from current RA->readInStream positions; restore each stream's tellg/rdstate afterward. Do NOT advance stock downstream effects or consume/read globally assigned chunks. Maximum32768 paired records/window, configurable smaller only for tests. No full ReadAlign replicas.
2. Use actual stock readLoad (readLoad.h) into a LIGHT local preparer: numeric/raw/name/quality arrays, lengths/read ordinal/readFilesIndex/filter/name-extra, and copies of RA->clipMates vectors. readLoad itself has no downstream alignment. Copying ClipMate basic state is possible (see ClipMate.h); reject CR4/type>=10/unsupported mutable global profiles rather than sharing cr4 pointer work. No mutation of original qualHist/stats/RA/read state. Restore streams even on no candidates/EOF. Do not try to snapshot entireRA. Use exact field/buffer limits from source, not invented identifiers.
3. Combine mates and construct Read1[0]/position-wise complement exactly as stock oneRead lines35–72, using stock complementSeqNumbers, then qualitySplit exactly as mapOneRead lines16–23 on owned numeric bytes. Compute Nstart/Lstart and initial eligibility exactly from mapOneRead; up to4 chains per ACTUAL piece. Copy/lift CPU prefix-narrowing from maxMappableLength2strands lines22–96 as a PURE preparer returning either actual inner tuple or CPU-only short/direct decision. No maxMappableLength or storeAligns during preparation. Preserve field/read/generation/ip/fragment/istart/direction identities.
4. Submit the WHOLE window's candidates to ONE coordinator, not from lookup. Coordinator queues multiple windows, batches65536 from ready windows into existing usi backend, full batches only; resolve remainderCPU with a bounded progress/tail policy, no allworker barrier. Correctly handle windows smaller than64k so an arriving second window can combine (test two40000candidate windows with staggered arrivals). Hold bytes/results stable until worker retires window. Test real batching/routing against a bounded backend test double; never label itGPU evidence.
5. Original mapChunk then calls ordinary RA->oneRead unchanged. Hook mapOneRead generation/piece/istart/Lmapped context and original inner call site after prefix. lookup only checks completed window candidate exactkey+bytes, updates maxL,Nrep,range; no submissions. Never admit adaptive Lmapped>0. Strict stock oracle remains. Advance/retire window by actual read ordinal without changing RNG/workspace/source ordering.
6. Suppression gate instrumentation on original mapOneRead counts opportunities/suppressed and labels actuallyunused reverse istart0 candidates by read/piece. Record actual GPU stats for suppressed unused work and hit/miss/unused in sidecar. Do not feed hypothetical floor counts into output.
7. Add REAL window/coordinator tests and source/actual C++ compile, not only string-presence tests. Add Makefile objects and staged hooks/headers needed for new mapChunk/mapOneRead functions. Keep compileflag default-off. Finish enabled source readyforparentgate(i), no further 'foundation only' handoff. If a specific leaf API resists, isolate that exact compiler/error instead of declaring the entire planner unimplemented.

One Astra consumption-order review comes only after a functioning window exists. Do not spend another delivery on inner-call rendezvous. The missing artifact is the bounded stock-parser/prefix lookahead and completed-cache consumer.