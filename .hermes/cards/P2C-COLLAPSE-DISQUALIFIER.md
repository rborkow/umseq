# P2C-COLLAPSE-DISQUALIFIER — opt-in load-time layout experiment

User authorized STAR follow-through 1–7, including an evidence-backed final optimization attempt. You are Terra, scoped implementation worker. Read AGENTS.md; source beats card silently; stop only if source and golden disagree. Do NOT run anything on Spark, commit, push, or change data/runs. Format generated C++ snippets with clang-format and match Python style before finishing. Other work in this dirty tree is the orchestrator's; preserve it.

Evidence: bench/PHASE2C-fresh-timing.md, bench/PHASE2C-fresh-profile.md, bench/evidence/residency-lat-close/verified-residency-summary.json. Accepted binary full-depth parity is green. Latest three rotated repeats: GPU 645.08 CPU-s vs advised CPU 683.46 (−5.62%, not ≥8%). Profile showed extra non-search CPU; do not assume it is removable. Same-binary residency pair just verified: three large advised VMAs ~98.995% huge-backed in bypass vs94.72–94.78% in GPU; the SAindex-sized VMA 92.947% vs47.947% initially. This is a real confound, not proof of causality. No new performance claim yet.

Hypothesis: explicitly collapsing already-loaded, advised index pages may stabilize backing and improve the controlled comparison; it may instead cost more CPU/setup time than it saves or fail under fragmentation. Cheap disqualifier only, not production allocator hardening.

Own ONLY bench/star-integrate/make_star_integrate.py; a NEW focused test file bench/star-integrate/test_collapse_patch.py; NEW bench/PHASE2C-collapse-disqualifier.md. Read source patch generator and actual generated STAR definitions/usages before editing. Do NOT edit runtime coordinator/window files, CUDA/Rust backends, other tests or measurement runners. Do not reformat the entire dense generator.

Implement a narrowly scoped Linux-only, opt-in runtime flag STAR_INTEGRATE_COLLAPSE_INDEX=1 (unset/0 off), compiled into the existing generated STAR integration. Require THP advice enabled too; THP=0 must remain the genuine no-advice/no-collapse arm. After the three index file reads and their existing explicit cache-eviction calls, and before GPU initialization/mapping, optionally issue MADV_COLLAPSE for the three resident index arrays. Trace real pointers/extents: never guess fields, use G1/allocation extent as appropriate, SA.charArray and SAi payload fields only after verifying their real allocations. Restrict to complete 2MiB-aligned extents wholly inside each owned allocation, avoid endpoints and arithmetic overflow. Do not touch STAR content, packed arrays, alignment flags, timing boundaries, ordinary allocation, cache default or ABI. No cudaHostRegister, HugeTLB reservation, global drop_caches, retries or fallback allocator.

Expose result/errno and elapsed wall time per collapse call on a prefixed diagnostic stderr line, including range bytes and whether skipped/attempted. Advice absent flag must take no collapse action. A syscall failure remains visible and falls through without fabricated success; orchestrator rejects failed-collapse arms for the intended intervention, while preserving their diagnostic cost. Add no output normalization or parity relaxation. Do not promise full coverage from madvise success: on-host smaps is the acceptance measurement. MADV_COLLAPSE may need Linux constant 25 behind a guarded definition; guard Mac builds cleanly.

Write RED/GREEN focused generated-source/runtime mock tests: default-off, explicit-on, THP=0 override, load/drop/collapse/GPU-init ordering, pointer/extent arguments, full-2MiB interior alignment, too-small/empty span skip, failed syscall result reporting. Use actual generated helper for runtime tests, not a reimplementation. Verify existing test_source_patch.py and test_window_contract.py still pass. No huge real allocations or collapse calls in local tests.

Return exact files, tests actually run, and limits. The orchestrator will independently review, freeze a new candidate, re-gate it on Spark and perform a same-binary collapse-off/on CPU/GPU ablation with collapse time INCLUDED. Repeated timing and comparator symmetry are mandatory; no gate is met by this tooling delivery.
