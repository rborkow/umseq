# P2C-SEED-GPU-PROBE dispatch — Astra algorithm-led performance probe

Workdir /Users/rborkows/projects/uni-rnaseq (main). Read the authoritative .hermes/cards/P2C-SEED-GPU-PROBE.md and P2C-ROADMAP.md, both docs/review-seed-triage.md (3926f49) and docs/review-seed-replay.md (69c3bca), then the algorithm/API sources the original card lists. User accepts review2: decide GPU viability BEFORE correctness scaling. This is PROBE only, not SEED-CUDA or a correctness gate. Routing adjustment: Astra owns the packed-SA, GstrandMask, reverse-coordinate and four-direction kernel details plus faithful CPU mirror; reuse existing infrastructure for minimal loader/harness. Orchestrator handles Spark compilation/integration fixes; don't build another review ladder or general framework.

Implement original card's main-repo files, complete local tests/stub build and provide concrete Spark commands. One-request-per-thread first; make variant selection explicit and leave one-request-per-warp as conditional on first measured GPU being no more than3x slower thanCPU20. Do not invent performance to decide this conditional; leave a clear continuation if actual measurement is needed. CPU20 = good Rayon implementation of source search.cpp over SAME resident buffers/requests, no per-request heap allocation or full-index scans. Preserve algorithm endpoint/history/tie/range expansion. Whole-index/metadata validation belongs once BEFORE timing, not search.cpp's tiny validation scan repeated per request. Probe correctness smoke compares every four-output tuple AND signals bounds/unsupported requests; report mismatch and stop. Same algorithm on both sides is not upstream oracle proof.

IMPORTANT preflight corrections already measured:
- Pinned STAR parametersDefault has seedSearchLmax=0, meaning unlimited (line575), NOT50. Respect source: requests use remaining read length or explicitly label a50-base synthetic cap, not a STAR default. Favor remaining length with clear provenance.
- Full index genomeParameters.txt contains '### GstrandBit 32', genomeSAindexNbases14, sparseD1, versionGenome2.7.4a (index format version, not installed STAR executable version), sjdbOverhang100, sjdbInsertSave Basic, genomeFileSizes3263383838 25352264482. Actual files Genome3263383838, SA25352264482, SAindex1565873619 bytes. nGenome/nSA/masks are derived using pinned loader/PackedArray semantics, NOT fictional named keys in that file. Annotation-generated SJ is present; don't silently exclude it or treat on-disk sjdbInsertSave Basic as a mapping-time two-pass command. Reject unsupported state with exact cause.
- Main build.rs currently compiles only umgpu_shim.cu; you own needed minimal additional probe translation-unit compilation, feature propagation and Mac stubs. No replacing/removing established kernels. umgpu already linksstdc++, CUDA13CCCL include. cuda feature in new crate must forwardumgpu/cuda. No unapproved umem API edits: report real missing API for orchestrator.
- A 4K control is Allocation::Anon{huge:false,require_huge:false} with measured coverage0, NOT merely turningrequire_huge off. Require100% verified huge backing for random-access arrays in primary probe, flag HugeTLB fallback. Allocate largestSA first, throughumem only; no cudaHostRegister/cudaMemcpy/index staging. Log actual CPU/GPU pointer/lease provenance as well as bytes_copied==0 (which is a constant in current stats module, not a dynamic detector).

Ownership per original card: crates/umseed-probe/**; probe additions to crates/umgpu (shim/seed_probe.cu and needed ABI/header/src/cuda.rs/stub.rs/lib.rs/build.rs); rootCargo.toml workspace member and minimally generated Cargo.lock if needed. No umb am/umem changes, no docs/bench/Kanban/original-card writes, no seed-worktree edits, no commits. Put implementation report in crates/umseed-probe/PROBE-IMPLEMENTATION.md. Every new artifact/module/result/probe command should identify PROBE; do not relabel old shared APIs. Carry LICENSE.star. Standard repo dependencies only after reading manifests. Rust unsafe only in umgpu's documented lease/FFI boundary (custom Pod impls there), no unsafe Rust in new binary. cargo fmt and readable multilineC++; use tests-first for key packing/directions/CPU/kernel transport. No installation/network/SSH/GPU execution from worker; orchestrator does host rounds.

Measurements to support, not fabricate: deterministic request file from real20Mmate1 bytes, forward+reverse, complementarys[1], request sizes64000/256000/1000000/4000000 (make actual numeric counts explicit; if powers-of-two chosen document unambiguously); at least3 repeats each; CPU20 andGPU consume same requests. Prefer source-correct prefix interval if straightforward; otherwise SA_SEARCH_FULL with conspicuous caveat (not actual captured STAR scheduler requests). Expose counts, request/index hashes, actual worker thread count, device-wall and event timing, warmup (~1secclocks), logical SA gathers and compared bytes/loops, means/max and per-warp imbalance. Exclude load/setup from warmcompute ratio but record them separately; do not hide GPU launch/fence cost. Throughput ratio GPU_RPS/CPU20_RPS = CPU20time/GPUtime (larger is better). Unconditional per-request output consumed by host checksums prevents dead-load elision; orchestrator will inspect SASS loads. Page sensitivity and simultaneousCPU/GPU disjoint halves required after agreement, with combined throughput and slowdown correctly stated. No fixed-asset capacity claim fromprobeRPS alone.

Coordination: Terra SPLIT is already active in seed worktree. It measures real20M/5M inner/direct/short compared-byte split. If inner<70%, orchestrator will steer you to ADD DIRECT-EXTENSION REQUEST TYPE before FINAL probe measurement. Keep request ABI extensible enough for a tag and source-equivalent direct extension without prematurely building it. Do not perform final measurement or infer boundary before SPLIT arrives. Main BAM/QC queue exhausted; no competing authorized worker edits here.

User expanded permission to downstream cards and Spark until today8pm. Conservative enforced cutoff =2026-09-06 20:00 America/Los_Angeles(PDT); this worker is supervised. It does NOT let you launch downstream cards; orchestrator applies evidence gates first and stops at cutoff. Spark86GiB available, GPUidle208MHz,32GiB HugeTLB pool free,1.8TiB diskavailable at preflight. Full index path /home/rborkows/uni-rnaseq/data/index/star_full. Shared heavy-job lock /home/rborkows/.cache/uni-rnaseq-resource.lock; SPLIT hostwork runs first, PROBE loads after, never concurrent full-index jobs. All host scripts must use fresh roots, exactprovenance and absolute cutoff; no service changes. Leave local tested code and explicit build/run commands for orchestrator; missing realGPU verification is honest, missing functional CLI/harness is not a completed delivery.

Deliver promptly: changed paths, actual Mac cargo test/clippy output, PROBE TSV schema and run commands, unsupported regimes, any needed API gap. No statisticalsuccessodds claims, inventedresults, shrinking realindex, or presumption that probe agreement is the correctness ladder.
