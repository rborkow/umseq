# INTEGRATE-1 — Terra implementation (contract is frozen and verified)

Read bench/star-integrate/CONTRACT.md and usi.h FIRST, then docs/STAR-INTEGRATE-DESIGN.md current decisions/on-ramp and bench/PHASE2C-chain-position.md. Astra delivered the actual C ABI; parent reran C11/C++17/all-offset checks and existing probe_transport test, PASS. Keep the frozen API. Implement real code, not a stub or advisory report. No kernel/search tuning; search is accepted. Parent runs Spark/20M gates after your delivery and ONE Astra consumption-order review.

User scope: mechanism(a) ONLY; no grid or continuations. Private compile flag/default-off STAR copy, duplicate G/SA umem context (no allocator ownership patch); per-worker bounded initial-start windows after stock qualitySplit and CPU prefix preparation. Enumerate istart0,1 x iDir0,1 for actual eligible pieces (Nstart/seedMapMin govern; combined pair has mate/spacer pieces, not literally four candidates total). Short/direct branches CPU. Coordinator aggregates64k,256k only if existing chunk capacity permits; underfilled tails CPU. Exact COMPLETE key lookup at original inner maxMappableLength callsite; original stock function fallback and strict parity oracle. storeAligns untouched. Worker/read/chunk/RNG downstream ownership and consumption order unchanged. No per-read ReadAlign workspace replicas; compact prepared bytes/keys/results only. No early alignment side effects. Strict oracle mode must actually use GPU results, compare every consumed4-tuple to original CPU with independent output storage, fail fatal on mismatch. CPU-only bypass must NOT pass the gate masquerading as integrated.

OWN: new crates/umstar/ Rust staticlib backend implementing usi.h; root Cargo.toml/Cargo.lock for registration; minimal new crates/umseed-probe/src/lib.rs and manifest library exposure if needed to REUSE existing cpu/index loader modules (do not change search algorithm). bench/star-integrate/ implementation/source generator/test/build doc files EXCEPT frozen CONTRACT.md,usi.h,abi_compile.c,test_abi.sh; leave run_host.py reserved for parent. Minimal umgpu backend API/build registration edits only if genuinely required; do not edit frozen probe_abi or kernel algorithm. No KANBAN/docs/.hermes/evidence edits. No commits, SSH, services or actual large-index jobs. Use workspace-write local tests and actual pinned-source compile checks. Native Mac has /opt/homebrew/opt/llvm/bin/clang++, clang-format, libomp at /opt/homebrew/opt/libomp. Rust cargo available. Deadline Sept7 20:00PDT via parent.

Practical backend reuse: crates/umseed-probe/src/index.rs::probe_load(dir,false) already verifies packed/full pinned layout, validates/hash-binds resident buffers, allocates actual required HugeTLB. Expose/reuse existing module rather than reinvent loader. It also loads SAindex; validate as needed then drop duplicate SAindex per frozen contract. probe_load uses crate::cpu::probe_packed_at; library can expose those existing modules without modifying algorithm. Backend identity must compare STAR loaded snapshot identity supplied by caller with loaded data, not merely echo directory hashes. Borrow caller data synchronously, materialize in owned umem, existing umgpu CudaExecutor/thread transport, output/result stats copyback; no retained caller pointers. Preserve contract checks/errors/drain semantics. Test non-CUDA path honestly (unsupported, never silently synthetic GPU). GPU execution is parent's host test.

Private STAR source generator: bench/chain-position/make_chain_position.py and external /Users/rborkows/projects/uni-rnaseq-seed/experiments/star-seed/replay/real_index_oracle.py show full344-file inventory/pinned-exact-patch practice. Pristine full source /private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source, host equivalent /home/rborkows/uni-rnaseq-seed-lab/real-index-input-2.7.11b/source. Source generator must accept --source/--tooling/--private-root, preserve pristine installed/source trees and prepare baseline and integrated copies plus diff/inventory. Mirror actual source; do not guess ReadAlign/ClipMate fields. Native compile syntax prerequisites: xxd -i parametersDefault -> parametersDefault.xxd, -DCOMPILATION_TIME_PLACE string, C++17 for new code, libomp headers. Normal Spark make preserves upstream flags, adds only include/link/compile flag; never globally override CPPFLAGS/CXXFLAGS suppressing HTSlib. Frozen usi.h relative probe-header include must resolve in staged build.

Progress: bounded coordinator queue, no every-worker barrier/EOF deadlock; full batches must actually form. Do not immediately send all windows CPU simply because a first worker arrives alone. Use sizeable worker windows, safe condition/progress policy, CPU tails when no full batch can form without starving active workers. Reuse request/read buffers after completion; no window overwrite in flight. Keep original parsing/qualitySplit/read normalization (Read1[1] is complement, not Read1[2]); preserve original stream position and mutable clipping/counter state if using lookahead. Profile-ineligible modes choose visible CPU bypass and cannot satisfy gate(i).

Telemetry in separate integration sidecar ONLY (leave Log.final.out unchanged): actual inner calls, submitted/unique/GPU consumed/unused, key misses, rejected/faults, CPU fallback; compared-byte work for hits/misses/unused and gather totals where available. Strict oracle instrumentation can count CPU fallback bytes in parity mode, but don't leave gratuitous per-byte overhead in future timing mode. Suppressed reverse istart0 candidates counted using actual suppression gate; unused work attributed from actual GPU stats. Reference floor13435368/159989084 chain opportunities is not itself a predicted GPU-request percentage. Count suppression opportunities independently so reconciles CHAIN baseline. Record prefix/materialization/device/lookup costs and startup; no fabricated main-only loops.

Required local tests (TDD, actual producer/consumer paths, format): ABI reuse; context/input/identity/epoch/failure and buffer lifetimes; exact-key fields/bytes/generation collisions; candidate split/mate/short/direct admissions; result routing/status/bounds; injected wrong tuple fatal; EOF/underfilled/no-worker deadlock and bounded full-batch dispatch; unchanged storeAligns and source-patching guard. Run tests and compile against ACTUAL STAR source, not just stub structs. Give parent precise release backend + private STAR build commands, runtime env switches/defaults, telemetry schema and small/full gate command recipe in IMPLEMENTATION.md. Parent reuses accepted stock artifacts and unchanged seed_split_parity.py on20M, then reports before any timing runs. Mark anything untested honestly. Do not stop merely because GPU cannot run locally: host execution is explicitly parent's next step, deliver all implementation through that boundary.