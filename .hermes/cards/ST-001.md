# ST-001 — Spark tiered-storage follow-through

## Context
Read `docs/STORAGE.md` first. Everything else you need is in this card.

We are moving umseq/biocoder artifacts off the DGX Spark's NVMe onto a NAS: T1 = NFS `/mnt/llm_work` (≈900 MB/s), T2 = rustfs S3 via `rclone rustfs:` (config already has `no_check_bucket = true`). Tools `~/bin/tier-promote`, `~/bin/tier-archive`, `~/bin/tier-df` are installed on the Spark (`~/storage-scripts/`, same as `scripts/storage/` here). rclone is `~/.local/bin/rclone` — put `$HOME/bin:$HOME/.local/bin` on PATH in every ssh command.

Host access: `ssh Sparky` (Linux aarch64, user rborkows). Foreground ssh dies after ~7 min: anything long runs as `nohup timeout Nh script > log 2>&1 &` and you poll the log. **No sudo is available** (password required). The uid/gid on the NFS share is squashed to 3000: never `rsync -a` to `/mnt/llm_work`; use `rsync -rltDH --no-perms --chmod=ugo=rwX`.

Two background jobs are ALREADY RUNNING on the Spark. Do not start them again:
- `~/phaseB.sh` → `~/phaseB.log` — mirrors `~/uni-rnaseq/data` to `/mnt/llm_work/umseq/data`, then `tier-promote`s every dir under `~/uni-rnaseq-probe-lab/*/` and `~/uni-rnaseq-seed-lab/evidence/*/` into `umseq/evidence` (and `umseq/evidence/seed-lab`), then deletes `~/uni-rnaseq/runs/*/work` and promotes the runs into `umseq/goldens`. Ends with a line `== DONE`.
- `~/phaseB5.sh` → `~/phaseB5.log` — `rclone sync --links --checksum` of six HF cache dirs to `rustfs:llm-models/hf/<dir>`, printing `VERIFIED <m>` or `CHECK FAILED <m>` each. Ends with `== DONE`. `models--RadixArk--Qwen3.8-27B-DSpark` printed CHECK FAILED because one file was root-owned at the time; ownership has since been fixed (`chown` via a docker alpine container), so it just needs re-syncing.

## Deliverables (in order; stop and report after any step that resists 2 attempts)

1. **Wait for both jobs** — poll `tail -3 ~/phaseB.log ~/phaseB5.log` every 2–5 min until each shows `== DONE`. Don't proceed while either is running (they saturate NVMe).
2. **Verify Phase B**:
   - `grep -c "^OK" ~/phaseB.log` and `grep -E "VERIFY FAILED|refusing|not a" ~/phaseB.log` (expect none).
   - `ls -l ~/uni-rnaseq-probe-lab | grep -c '^l'` should equal the number of dirs that existed (count non-symlink dirs remaining too; loose files at the root are expected to stay).
   - Every path cited in bench docs must still resolve. Run **locally** (Mac, in this repo): `grep -rhoE '~/uni-rnaseq-(probe|seed)-lab/[A-Za-z0-9_./-]+' bench docs | sort -u > /tmp/cited.txt`, the file is already staged at `~/cited.txt` on the Spark (19 paths); there run `while read p; do p="${p/#\~/$HOME}"; [ -e "$p" ] || echo "MISSING $p"; done < cited.txt`. Report the MISSING list verbatim (some may have been deleted before this work — that's information, not something to fix).
   - `grep "B2 .* OK" ~/phaseB.log` shows reference, index, samples all OK.
3. **Finish models (B5)**: re-run the sync for DSpark only: `cd ~/.cache/huggingface/hub && rclone sync --links --checksum --transfers 8 --s3-chunk-size 256M --s3-upload-concurrency 4 models--RadixArk--Qwen3.8-27B-DSpark rustfs:llm-models/hf/models--RadixArk--Qwen3.8-27B-DSpark && rclone check --links models--RadixArk--Qwen3.8-27B-DSpark rustfs:llm-models/hf/models--RadixArk--Qwen3.8-27B-DSpark`. Then, **only for dirs whose `rclone check --links` reports `0 differences`** (re-run the check yourself for all six, don't trust the log), `rm -rf` the local dir. The six: `models--Qwen--Qwen3-1.7B models--RadixArk--Qwen3.8-27B-DSpark models--z-lab--Qwen3.8-27B-DFlash2 models--RadixArk--Qwen3.8-27B-NVFP4 models--Qwen--Qwen3.8-27B-FP8 models--Qwen--Qwen3.8-27B`. **Never touch `models--RadixArk--Qwen3.8-Flash-Next-NVFP4`** — it is being served by the running container `qwen38-flash`.
4. **Docker**: `docker image prune -f; docker builder prune -f; docker volume prune -f`. Then save the dormant sglang image before removing it: `docker save lmsysorg/sglang:qwen38-27b-dflash2 | zstd -T0 -3 | rclone rcat --s3-chunk-size 256M --s3-upload-concurrency 4 rustfs:artifacts/docker/sglang-qwen38-27b-dflash2.tar.zst` (run under nohup; ~57 GB in, expect 20–40 min). Verify with `rclone size rustfs:artifacts/docker/sglang-qwen38-27b-dflash2.tar.zst` > 10 GB, then `docker rm qwen3.8-27b-sglang` (its exited container) and `docker rmi lmsysorg/sglang:qwen38-27b-dflash2`. Do **not** stop or remove `qwen38-flash` or `spark-searxng`. Keep all `community.wave.seqera.io/*` images.
5. **Biocoder private material → S3**: for each of `~/biocoder-private-results ~/biocoder-eval-staging ~/biocoder-private-staging`: `rclone sync --checksum <dir> rustfs:artifacts/biocoder/<basename>` then `rclone check`. Leave the local dirs in place (next cycle will confirm the launcher reads from S3). Delete scratch: `rm -rf ~/biocoder-spark-build-* ~/biocoder-spark-launcher-v* ~/biocoder-spark-locks ~/biocoder-out`.
6. **Cron** on the Spark (`crontab -e` non-interactively via `crontab -l | { cat; echo ...; } | crontab -`):
   - `15 3 * * 0 find /mnt/llm_work/_trash -mindepth 1 -maxdepth 1 -mtime +14 -exec rm -rf {} + 2>/dev/null`
   - `30 3 * * 0 PATH=$HOME/bin:$HOME/.local/bin:/usr/bin:/bin $HOME/bin/tier-df > $HOME/tier-df.last 2>&1`
   Confirm with `crontab -l`. Run `tier-df` once by hand and include its output in the report.
7. **Report**: `df -h /` before/after (before was 2.0T used / 1.6T free), `du -sh` of each dir you removed, the MISSING list from step 2, `rclone size` for `rustfs:llm-models`, `rustfs:artifacts`, and `docker system df`.

## Constraints
- Do not modify anything in this repo except to add nothing — this card is host ops only; write your report to stdout (it is captured in the card log).
- Do not touch `~/uni-rnaseq/data` (the hot mirror stays), `~/uni-rnaseq-data`, `~/micromamba`, `~/.cache/uv`, or anything not named above.
- Never `pkill -f` inside an ssh string. Never `rm` before the matching verify has printed clean.
- No `rsync --delete` anywhere.
- Finish with a summary: **done / blocked / needs-human**.
