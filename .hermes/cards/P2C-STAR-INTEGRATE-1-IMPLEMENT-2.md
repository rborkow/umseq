# INTEGRATE-1 continuation — Terra: finish the actual STAR scheduler/consumer

This is the SAME implementation card, not a new review/card. Read .hermes/cards/P2C-STAR-INTEGRATE-1-IMPLEMENT.md and frozen bench/star-integrate/CONTRACT.md. Your first delivery built the backend but left star_integrate.hpp as lookup=false. Gate(i) remains NOT runnable. Do not rescope as another backend foundation report. The only remaining deliverable here is the complete enabled private STAR C++ path and its actual tests/build recipe. Parent owns Rust backend corrections while you do this; DO NOT edit crates/**,Cargo*,frozen headers/CONTRACT or run_host.py.

OWN bench/star-integrate/ C++ implementation, generator, test files, IMPLEMENTATION.md except frozen ABI files and parent run_host.py. Source is the pinned full tree already named. Actual libumstar/usi ABI now exists. Implement bounded per-worker lookahead within original assigned chunk; prepare actual numeric mate-combined Read1[0]/complement/qualitySplit and original CPU prefix decisions; collect istart0,1 x iDir0,1 for actual eligible pieces; do not run short/direct branches on GPU; keep original storeAligns/adaptive/suppression/downstream/RNG effects exclusively in stock consumption. Compact window, NOT per-read full workspace copies. A dedicated per-worker scratch parser/preparer is acceptable if bounded and does not change global/per-worker downstream state; read raw buffers/streams with exact stock parsing/clip conventions or reject unsupported profiles. Do not assume two clean75bp mates or seedStart counts blindly.

One bounded coordinator calls usi_init/batch/destroy, actual64k aggregate GPU dispatch (256k only if capacity permits), safe EOF/tail/backpressure progress, CPU underfilled tails. Avoid a policy that falls back all first-arriving windows before others can contribute. Full key includes complete actual tuple,immutable read bytes,generation,piece/index identity; preserve stock source consumption order. Lookup must update maxL as well as Nrep/interval; current stub signature lacks a maxL ref. Strict mode GPU tuple chosen/consumed with originalCPU oracle independently comparing4fields; fatal on mismatch even when fallbackSAMsame. Status/faults visibly fallback/disable; cannot claim gate on allCPU bypass.

Initialize backend identity from actual STAR-loaded G/SA/SAindex snapshot, using existing SHA256 helper from accepted replay tooling as needed, disk file extents only to choose hash span after checked buffer bounds. CUDA backend is linked Rust staticlib. Frozen header include paths must resolve from actual generatedsource. Compile flag STAR_INTEGRATE=1 distinct from env runtime; default build remains stock. Keep telemetry separate fromLog.final.out: hit/miss/unused work and counts, suppression opportunities and actual unused suppressed-reverse GPU work (reference13435368/159989084), prefix/materialization/lookup/device/startup costs. Mark booleans/modes explicitly. Leave all held work untouched.

Tests: actual coordinator full64k dispatch via test backend, routing/out-of-order worker arrival, EOF/tails/retirement, key mismatch and byte identity, wrongtuple fatal, eligibility and actual producer sidecar reconciliation. Prepare real pristine source and compile enabled translation units, not stub structs alone. Run the original ABI tests without edits. Update IMPLEMENTATION.md honestly, with exact Spark make/link/runtime commands and sidecar schema. Do not stop because hostCUDA cannot run onMac; that's parent's responsibility AFTER delivery. No SSH/commits/services/review. Format with /opt/homebrew/opt/llvm/bin/clang-format. Deliver the working enabled path this time, not another skeleton.