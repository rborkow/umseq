# SAME INTEGRATE-1: Terra finish coordinator/cache consumer (producer is another worker)

OWN bench/star-integrate/star_integrate.cpp, make_star_integrate.py, test_source_patch.py, new test_coordinator.cpp/.py, IMPLEMENTATION.md. Header star_integrate.hpp may only gain byte-counter declarations if necessary; preserve parent's WindowRead APIs. DO NOT TOUCH star_integrate_window.cpp or test_window_prefix* (concurrent Terra finishes its pure prefix stage), Rust/Cargo/frozen ABI/run_host.py. No SSH/commits/review.20:00PDT cutoff. Read current header and coordinator first, not all historical cards. Source /private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source. This is the missing concrete consumer/coordinator, finish it, no new spine-only delivery.

Parent froze internal API: WindowRead{ordinal,a,b,candidates} owns bytes once/frame. Producer scans up to32768chunk records with real readLoad/qualitySplit/CPU prefix then calls submit_window(vector<WindowRead>&&) ONCE after restoring streams. Its candidates have exact InnerCall fields piece/fragment/nstart/lstart/istart/start/length/prefix/interval/dir/distance0; generation initially0. window_remaining() before next stock oneRead avoids rescanning live frames. Empty frames retained for actual read ordinals. begin_map removed from producer, YOU implement it.

Replace current lookup's submit-and2ms-wait (can never batch20blockedworkers) with:
- Thread-local window owns frames, jobs/results/stats, current read cursor, generation, original chain context and per-window counters. submit_window transfers ownership then queues all its real inner candidates and waits until all jobs resolve. One dedicated coordinator thread owns usi_search_batch calls and consumes candidates from multiple windows. Full65536 batches (262144 only if available); <65536 waits bounded ~2ms for anotherwindow then resolvesCPU, no mandatory allworker rendezvous. No per-candidate duplicate a/b vectors, borrow frame arrays into batch until synchronized completion. Pending pointers owned until resolved/retired. setup called from prepare_window by producer; initialize service once. Drained shutdown/failed-batchCPU with no stale publication.
- begin_map(ReadAlign&) selects next prepared frame by real ra.iReadAll, never modifies RA. set_chain(piece,fragment,istart,nstart,lstart,lmapped) records real original loop context; hook mapOneRead before original maxMappableLength2strands call with ip,splitR[2][ip],istart,Nstart,Lstart,Lmapped. Do not search adaptive Lmapped>0. lookup augments original inner call with chain identity/current generation and requires full tuple AND a/b byte equality and unconsumed completed validresult. Lookup cannot enqueue/work/wait. Publish length/count/low/high only then; stock fallback/oracle/storeAligns unchanged. Use TLS counters without per-hit global mutex, aggregate once/window; no global lock across every STAR inner call.
- end_chunk hook retires remaining unused candidates before stream/chunk turnover. window_remaining retires completed prior window. Mainthread0 window must also retire before finish; workersEOF/noentries must progress. Profile admission rejects unsupported WASP/remapping/CR4 modes if preparer cannot mirror them. Check exact source, no invented field names.
- Hook reverse_suppressed(ip) where ORIGINAL mapOneRead sets flagDirMap=false at lines74–75; do not correct the upstream Shift+L==splitR[1][ip] predicate! Mark that frame/piece's reverse(iDir1, therefore dirRfalse) istart0 candidate suppressed, tally raw opportunities/candidates/unused GPU work. Reference13,435,368/159,989,084 labels remain comparison ONLY, never synthetic measured output. Early direct branches can suppress reverse too.
- Emit actual GPU batch sizes/counts, candidates/CPUtails/hits/misses/unused, their compared bytes/gathers, suppressed unused counts/work. For fallback inner compared bytes: reuse accepted counter producer's RETURN-level compareSeqToGenome hooks (bench/chain-position/chain_position_hooks.hpp and make_chain_position.py; tools source source-of-truth), active only while original inner fallback/oracle is running. No per-byte callback overhead, no SSIR. Hit/unused work from returned ProbeStats. Strict mode already invokes stock oracle on each real GPUhit; countmismatches and abort on first with exact tuple diagnostic.
- Add actual C++ coordinator/window fixture against a clearly-labelled deterministic test backend, checking two staggered40000windows produce a64k GPU-dispatch-shaped batch, CPUtail, reordered window/read identity, key mismatch/adaptive rejection, suppressed unused, EOF/shutdown. Real GPU evidence is parent job, fixture isn't performance. Ensure producer/coordinator together compile from staged private source and default-off path builds.

One Astra consumption-order review ONLY after real producer/coordinator complete. Parent has built real RustCUDA backend successfully on Spark; no need to revisit Rust or ABI. Parent owns strict20M runner and gate(i); no timings. Format clang-format /opt/homebrew/opt/llvm/bin/clang-format. Return exact files/tests plus any genuine remaining compiler diagnostic, not an unimplemented coordinator.