# P2C seed lane coordination

## Current checkpoint — REAL-REQUESTS complete; STOP for decision

Verified `bench/PHASE2C-real-requests.md`: real **6.59556× CPU20**, fresh synthetic10.37769×; CPU and thread GPU each match **999,914/999,914 captured STAR tuples**, zero skipped. Original20-worker capture and full20M stock parity reused, not rerun. Raw audit: zero `N>S+1`;1,317 fully-known reverse-prefix cases corrected without changing SSIRv1/caps. Three repeats, disjoint overlap and4K control pass; parent independently checked every projection and61 measured source hashes. Immutable host1/source-v1 preserved; corrected host2 evidence archived locally and source-v2 preserved remotely.

The user's renewed three-hour grant was enforced to Unix1788756895 (21:54:55PDT), and the host runner completed normally. No successor work started. The design remains delivered; awaiting user decision on SEED-CUDA + STAR-INTEGRATE implementation. All previously held fault/owned/cap-matrix cards remain held. Older cutoff and pending-status statements below are historical, not active instructions.

## Latest user direction — representativeness before hardening

Main independently accepted SPLIT/PROBE from raw evidence and updates the hypothesis odds to ~75%. Hold (do not resume) PE-HOST-FAULTS/2, OWNED-SEARCH-SESSION, OWNED-REQUEST-TESTS and FULL-INDEX-CONTRACT cap-matrix work. Their workers have exited; no fault host run was launched. Preserve all results without more hardening.

The parallel design is delivered in main `docs/STAR-INTEGRATE-DESIGN.md`, with one independent pass incorporated and parent source/arithmetic checks. It recommends initial-start speculation plus exact CPU fallback, conditional on real timing and work coverage; no integration implementation. Terra's first REAL-REQUESTS delivery lacked the capture adapter and distributions; continuation `P2C-REAL-REQUESTS-2` (proc_8db074a8dc38) completes those within the same card. No measurement yet. One independent review per card. >=5x real requests supports remaining rigor; near2x changes the outlook. After REAL-REQUESTS, STOP for the user's decision on SEED-CUDA + integration implementation. Existing20:00PDT cutoff and shared lock remain. The older detailed checkpoints below are history, not successor instructions.

**Current: user handed both worker slots and Spark to the seed lane for SPLIT + PROBE, including evidence-gated downstream cards, until today's evening cutoff.** Enforced conservative cutoff: **2026-09-06T20:00:00-07:00** (20:00 Los Angeles/PDT; earlier than literal PST). Local workers have process-group deadline supervisors. Spark commands must use GNU timeout to the same absolute deadline; verified timeout test exited124. Shared lock `/home/rborkows/.cache/uni-rnaseq-resource.lock` remains mandatory, one heavy host job at a time. No services/settings changes authorized or made.

- Read both reviews `3926f49` and `69c3bca`; hold REPLAY-SCALE until PROBE≥2×. SPLIT determines whether inner ≥70% of compared bytes; otherwise add direct-extension request type before final PROBE measurements. A1.5–2× ratio remains a decision point; <1.5× produces a documented negative and parks.
- SPLIT measured and verified:20M/5M full-index paired runs, biological SAM/SJ/non-timing stats parity. Inner compared bytes99.78875%/99.78868%; CPU instrumentation overhead+31.80%/+27.36%, not corrected. Select inner-only. Seed `RESULTS-SEED-SPLIT.md` and evidence `seed-split-run2` + `seed-split-run2-continuation` retain raw mapping and metadata-only parity repair. `seed-split-run1` retains linker failure.
- Actual thread PROBE round completed under lock, exit0: `~/uni-rnaseq-probe-lab/probe-measure-host1`; local `bench/evidence/seed-probe-measure-host1`. Huge-page ratios~10× vsCPU20, best paired median10.20× at256k; 4K control0.0885× at64k. All same-algorithm output/counter comparisons pass; source/loaded array/request hashes, pointers, page reports and raw timings retained. NOT upstream correctness or STAR speedup. Probe's full-SA requests differ from captured STAR prefix intervals.
- PROBE closed: final `probe-warp-host4` exit0, both variants x4 batches x3 plus overlap and warp4K control. Thread selected:10.2052× CPU20 at1M,56.83M requests/s; warp best5.9456× at64k. Warp4K0.1337×. All44 source hashes, outputs/counters, repeats and overlap denominators verified; both SASS bodies60LDGs. Local archives preserve host1/2/3 failures and successful host4. Luna bounded review has no blockers; legacy request-byte field names are logical compared bytes and only wrapper-verified provenance counts as evidence. Report: `bench/PHASE2C-seed-probe.md`.
- REPLAY-SCALE: actual tiny PE run2 **28/28** independent four-output matches, stock parity, both fragments/directions and all-N zero-INNER pair. Astra `REVIEW-PE-FINAL.md` independently reproduced and approved bounded positive evidence. Historical SE-only guard subsequently exercised in `evidence/pe-old-guard-check2` (intended profile abort, not replay-parser rejection). Targeted host short-write/ENOSPC/crash checks remain before unconditional Slice1 closure; Terra's isolated harness is delivered for parent review/host execution. Parent corrected narrow brute-force semantics/coverage:120 cases/10 tests pass. Separately scoped owned-search implementation is under correction: parent fixed whole-SA copy per lookup (allocation RED4/32bytes→GREEN0/0) and restored readable formatting; request-domain coverage and pre-allocation budget remain held. Full-index/20M execution, source observation, rollover and CUDA remain separately gated. No silent cap removal, wire-version change or renewed human decision needed for approved bounded work.
- No SEED-CUDA, STAR integration or pipeline run approved by the probe itself. Orchestrator owns board, host work, evidence, and gate decisions. Workers cannot SSH/commit. Full index remains `~/uni-rnaseq/data/index/star_full`; installed STAR, main BAM/QC workspace, services/settings untouched. Spark was lock-free after completed PROBE; next host job is bounded tiny PE verification under the same lock/deadline.

## Historical tiny milestone (completed, preserved)

- `~/uni-rnaseq-seed-lab/read-capture-host1.status`: capture_passed_requires_review,exit0. Originals `read-capture-run1`; local archive `experiments/star-seed/evidence/read-capture-run1/` in seed worktree.
- Actual stock/capture mapping both exit0 for12SE reads on each no-SJ/SJ index. Captured109/106 INNER calls. Orchestrator fresh sanitizer parser rerun validates counts and strict parity rerun passes; raw SAM alignment and SJ bytes identical. This is not independent search replay.
- Capture and tiny integrated replay APPROVED. Luna re-review closed all three blockers and reproduced109/106 actual four-field matches; durable integrated-replay-v2 evidence. No further worker or remote job launched at this milestone.
- No PE, full human-index or performance/GPU result; tiny SE SSIR-to-loader/search integration is independently approved. Installed STAR, prior evidence/variants, main BAM/QC workspace, services/settings untouched.

The historical reservation release above is superseded by the current user handoff; the shared lock still governs every heavy successor job.
