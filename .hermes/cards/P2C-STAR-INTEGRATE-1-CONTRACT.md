# INTEGRATE-1 — Astra contract freeze, short, implementation authorized

Read docs/STAR-INTEGRATE-DESIGN.md current decisions and implementation on-ramp, bench/PHASE2C-chain-position.md, existing crates/umgpu/shim/seed_probe.h + seed_probe.cu and Rust transport before editing. Read actual pinned STAR source /private/tmp/star-full-source.UVdsuH/STAR-2.7.11b/source as needed. The transport and kernel exist and are accepted at6.60x,999914 tuples both arms. No kernel algorithm/tuning work. No new review rounds or revived held cards.

Latest user: CHAIN76.4% accepted. Mechanism(a) ONLY, no grid, continuations dropped. Freeze request/result contract and C-compatible header FIRST, short. Terra will implement duplicate G/SA umem context, per-worker bounded initial-start window after stock qualitySplit/CPU prefix preparation (istart0,1 x iDir0,1; respect actual piece/mate structure and Nstart; short/direct stay CPU), one coordinator64k aggregated batch,256k only if capacity allows, CPU underfilled tails. Exact tuple lookup at original inner call site, original stock function fallback AND parity oracle. storeAligns untouched. Keep worker/chunk/read/RNG ownership and consumption order. Gate(i) unchanged SPLIT SAM records/order/tags,SJ,non-timingLog.final.out on20M input BEFORE timing runs. Suppressed-reverse waste measured against13435368/159989084 chain opportunities. One Astra consumption-order review AFTER Terra delivers, not now.

OWN ONLY: new bench/star-integrate/CONTRACT.md, new bench/star-integrate/usi.h (if backend interface needed), new crates/umgpu/shim/seed_probe_abi.h, minimal top-of-header extraction edits to seed_probe.h and declaration reuse in seed_probe.cu if necessary for C-compatibility. New narrowly scoped C/C++ ABI compile test under bench/star-integrate/. Do not edit Rust implementation, build.rs or any other files. No commits/SSH/services/host jobs. Terra starts after your delivered contract, not concurrently. Deadline2026-09-07 20:00PDT enforced by parent wrapper.

Deliver actual C-compatible transport declarations/layout checks, keeping existing ProbeRequest80/ProbeOutput40/ProbeStats48/ProbeConfig24 layouts and symbols exactly. Separate C++ search implementation from C ABI, do not rewrite kernel. Freeze minimal synchronous owning backend interface Terra can implement using current umem/umgpu, borrowing caller arrays only during call, checked epoch/index identity, no caller pointers retained, explicit error/fallback semantics. Reuse existing transport rather than introduce another schema or ownership project. Header MUST compile as C11 and C++17 with offset/size assertions and actual tool output. If usi interface follows proposed design, avoid relabeling ProbeStats.loops as main-only: use existing correctly labelled counters. Keep telemetry in a separate sidecar, NOT extra Log.final.out fields (unchanged checker must work).

CONTRACT.md should be short and practical: final signatures/ownership; exact lookup key includes read generation, piece identity/context and complete original inner inputs/read bytes/index epoch (offsets alone are not identity); result order/status, inclusive bounds; candidate admission and per-worker reuse/lifetimes; strict parity mode CPU oracle on every consumed GPU tuple, mismatch fatal even if fallback yields sameSAM; no timing mode before gate(i). Note one synchronous coordinator, no worker barrier deadlock on EOF or small tails; no per-read workspace replication. List minimal tests Terra must write. Format new header code with /opt/homebrew/opt/llvm/bin/clang-format. Finish with paths, tests/output and implementation handoff, no speculative success claims.